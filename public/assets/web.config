<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>

        <!-- Disable directory browsing -->
        <directoryBrowse enabled="false" />

        <!-- Deny all users -->
        <security>
            <authorization>
                <deny users="*" />
            </authorization>
        </security>

    </system.webServer>
</configuration>